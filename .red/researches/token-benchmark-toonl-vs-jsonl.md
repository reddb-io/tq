# Token benchmark: TOONL vs JSONL vs closed TOON

This historical research note is superseded by the first-class benchmark suite
in [`benchmarks/`](../../benchmarks/).

The canonical deterministic token and byte reports are generated with:

```bash
pnpm benchmark:tokens
```

The current dated reports live under
[`benchmarks/results/`](../../benchmarks/results/). Keep new benchmark claims in
that directory rather than in `.red/researches/`, the root README, or published
package READMEs.
